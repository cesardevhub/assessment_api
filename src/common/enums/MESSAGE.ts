export enum FROM {
    AGENT = "agent",
    CLIENT = "client",
    OPERATOR = "operator",
    ADMIN = "admin"
}

export enum MESSAGE_TYPE {
    PRODUCT = "product",
    IMAGE = "image",
    DOCUMENT = "document",
    TEXT = "text"
}